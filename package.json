{
  "name": "node-base-hapi-api",
  "version": "0.0.1",
  "main": "server",
  "engines": {
    "node": ">=4.0.0"
  },
  "dependencies": {
    "code": "2.x.x",
    "handlebars": "4.x.x",
    "hapi": "^17.5.2",
    "hapi-swagger": "^9.1.1",
    "inert": "^5.1.0",
    "joi": "7.x.x",
    "lab": "7.x.x",
    "vision": "^5.3.3"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "lab -a code -c",
    "test-tap": "lab -a code -r tap -o tests.tap",
    "test-cov-html": "lab -a code -r html -o coverage.html"
  }
}
